# Demo 互動體驗系統 - 整合說明

## 📋 概述

已成功將互動式 Demo 體驗系統整合到現有的 Patient CRM 系統中，使用**原有的 UI 介面**進行引導教學。

## 🎯 實現方式

### 1. 使用現有系統 UI
- ✅ 疊加在現有的 Appointments（回診管理）頁面上
- ✅ 使用系統既有的組件（Card, Button, Badge 等）
- ✅ 保持原有的樣式和交互體驗

### 2. 三階段引導流程

#### Phase 1: 問卷調查（DemoSurveyOverlay）
- 全螢幕彈窗覆蓋
- 三個問題：診所規模、類型、痛點
- 動態進度指示器

#### Phase 2: 互動模擬（DemoGuideTooltip）
- 在實際 UI 上添加引導層
- 高亮目標元素
- 引導用戶完成實際操作

**Scenario A: 拖拉預約**
- 高亮病患卡片和目標時段
- 監聽拖放操作
- 完成後觸發撒花和打勾動畫

**Scenario B: 智慧標籤**
- 高亮新增標籤按鈕
- 引導點擊操作
- 顯示 AI 推薦

#### Phase 3: 價值轉換（DemoConversionPage）
- 全螢幕效能分析頁面
- 數字動畫展示提升效果
- CTA 行動呼籲

## 📂 新增的檔案

```
src/
├── types/
│   └── demoGuide.ts                    # Demo 類型定義
├── hooks/
│   └── useDemoGuide.ts                 # Zustand 狀態管理
├── utils/
│   └── demoEffects.ts                  # 特效工具函數
├── components/
│   └── demo/
│       ├── DemoSurveyOverlay.tsx       # 問卷調查彈窗
│       ├── DemoGuideTooltip.tsx        # 引導提示框
│       └── DemoConversionPage.tsx      # 轉換頁面
└── pages/
    └── Appointments.tsx                # 已整合 Demo 功能
```

## 🚀 如何啟動

### 方法 1: 通過按鈕啟動
1. 啟動系統：`npm run dev`
2. 進入「回診管理」頁面
3. 點擊右上角的「✨ 體驗互動教學」按鈕

### 方法 2: 通過 URL 參數啟動
直接訪問：`http://localhost:5173/appointments?demo=true`

## 🎨 視覺效果

### 已實現的特效
- ✅ 撒花動畫（canvas-confetti）
- ✅ 成功打勾動畫（SVG + CSS）
- ✅ 高亮脈衝效果
- ✅ 數字計數動畫
- ✅ Toast 通知
- ✅ 平滑過場動畫（framer-motion）

### 樣式系統
- 使用系統既有的 Tailwind CSS
- 使用 shadcn/ui 組件庫
- 動態高亮樣式注入

## 🔧 技術棧

### 新增依賴
```json
{
  "framer-motion": "^11.x",      // 動畫庫
  "canvas-confetti": "^1.6.0",   // 撒花特效
  "zustand": "^4.x"              // 輕量狀態管理
}
```

### 整合方式
- React Hooks（useState, useEffect）
- Zustand 全局狀態管理
- URL SearchParams 檢測
- DOM 操作（高亮、特效）

## 📝 使用範例

### 啟動 Demo
```tsx
import { useDemoGuide } from '@/hooks/useDemoGuide';

const MyComponent = () => {
  const { startDemo } = useDemoGuide();
  
  return (
    <Button onClick={startDemo}>
      開始體驗
    </Button>
  );
};
```

### 監聽 Demo 狀態
```tsx
const { isActive, phase, scenarios } = useDemoGuide();

if (isActive && phase === 'simulation') {
  // 執行模擬邏輯
}
```

### 觸發特效
```tsx
import { 
  triggerConfetti, 
  showSuccessCheckmark,
  highlightElement 
} from '@/utils/demoEffects';

// 撒花
triggerConfetti();

// 打勾
showSuccessCheckmark();

// 高亮元素
highlightElement('[data-demo-target]');
```

## 🎯 下一步工作（可選）

### 1. 完善模擬場景
- [ ] 添加更多實際的病患資料
- [ ] 實現實際的拖放操作監聽
- [ ] 整合 CustomCalendar 組件的 Demo 支援

### 2. 數據追蹤
- [ ] 整合 Google Analytics
- [ ] 記錄用戶完成率
- [ ] A/B 測試不同引導方式

### 3. 客製化
- [ ] 根據問卷答案動態調整場景
- [ ] 更多診所類型選項
- [ ] 多語言支援

## 🐛 疑難排解

### 問題：Demo 按鈕不顯示
**解決**：確認已經在 Appointments 頁面，並且沒有其他彈窗遮擋

### 問題：撒花特效沒出現
**解決**：檢查 canvas-confetti 是否正確安裝：`npm list canvas-confetti`

### 問題：狀態沒有更新
**解決**：檢查 zustand store 是否正確導入：`import { useDemoGuide } from '@/hooks/useDemoGuide'`

### 問題：高亮效果不明顯
**解決**：檢查目標元素是否有正確的 data 屬性（如 `data-demo-patient="1"`）

## 📞 技術支援

如有問題，請檢查：
1. 瀏覽器 Console 是否有錯誤
2. React DevTools 中的組件狀態
3. 網路請求是否正常

---

**建立時間**: 2026年1月24日  
**版本**: 1.0.0  
**維護者**: Patient CRM Team
